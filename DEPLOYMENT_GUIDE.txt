DERIV NEW OAUTH SETUP

Files updated:
1. index.html
2. api/deriv-token.js
3. vercel.json
4. bot-obfuscated.html

IMPORTANT STEPS BEFORE DEPLOYING:

1. Open index.html and replace:
   CLIENT_ID: "PUT_YOUR_DERIV_CLIENT_ID_HERE"

   with your real Deriv OAuth client_id from the Deriv developer dashboard.
   Your referral code 3655J683S288 is NOT the client_id.

2. In Deriv developer dashboard, register this redirect URL exactly:
   https://YOUR-VERCEL-DOMAIN.vercel.app/index.html

   If you use a custom domain, register:
   https://YOUR-DOMAIN.com/index.html

3. Deploy to Vercel.

4. If Deriv issued you a client secret, add it in Vercel Environment Variables:
   DERIV_CLIENT_SECRET=your_secret_here

   Optional:
   DERIV_CLIENT_ID=your_client_id_here

5. Your referral link is already set for the Create Deriv Account button:
   https://partner-tracking.deriv.com/click?a=20638&o=1&c=3&link_id=1

AUTH FLOW:
index.html -> Deriv OAuth login -> /api/deriv-token exchanges code -> /bot-obfuscated.html

NOTE:
The new Deriv OAuth2 flow returns a short-lived access token. If your bot still uses the old Legacy WebSocket authorize(token) method, you may also need to migrate the trading API calls to Deriv's new authenticated API style.
